<UserControl x:Class="KeywordComparison.Keywords"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KeywordComparison"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" x:Name="keywordlabel">Keywords</Label>
        
        <!--
            Like a modern frontend framework, we can add lifecycle hooks!
            For this ListBox we have an Initializer hook, when the box is created when the program is run
            When the program runs, we will call the function "readDataFile".
            What happens? Go check the code-behind file
        -->
        <ListBox Grid.Row="1" Grid.Column="0" x:Name="keywords" Margin="10" Initialized="readDataFile" Clos>
        </ListBox>

        <Button Grid.Row="2" Grid.Column="0" x:Name="removeButton" Click="removeKeyword"></Button>
    </Grid>
</UserControl>
